[pytest]
python_files = test_*.py
addopts = --html=report/report.html --self-contained-html -v
markers =
    validate_csv: marks tests that validate CSV file content (schema, values, format, duplicates)
    unmarked: automatically assigned to tests that have no explicit custom marks